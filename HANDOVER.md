# 쌉가능 인수인계 (4권) — 2026.08.23

준킴쌤(joon.kim@educclesia.com)의 중·고등 영어 문장 훈련 앱 **"이제 영어, 쌉가능"**.
작업 언어: 한국어. 다음 대화창에서 이 문서를 먼저 읽고 이어가면 됩니다.

> **3권에서 바뀐 것 (중요)**
> ① 프로젝트 폴더가 `~/Downloads/EC-trainer` → **`~/EC-trainer`** 로 이사
> ② 배포 방식이 GitHub 웹 업로드 → **터미널 `git push`** 로 전면 변경
> ③ `firestore.rules`가 3권 설명과 실제가 달랐음 (아래 7번 참조)
> ④ L7·L8 집필 완료 → **2차 임계점 콘텐츠 완성**

---

## 1. 지금 상태 (한 줄)

**12레벨 구조 + L1~L8 콘텐츠 집필 완료 + 라이브 배포 정상화.**
남은 일: **L9~L12 (3차 임계점) — 집필 전에 접근법 설계부터.**

---

## 2. 파일 · 경로

| 파일 | 줄 수 | 상태 |
|---|---|---|
| `index.html` | 1463 | 애플 디자인, 역할 4단, 레벨시험, 가이드 5탭 |
| `curriculum.js` | 3353 | **L1~L8 집필 완료** / L9~L12 골격만 |
| `firestore.rules` | 82 | **역할 2단(선생님/학생)** — 3권 설명과 다름 |
| `preview.html` | 182 | 로컬 커리큘럼 미리보기 (신규, 아래 9번) |

- 로컬: **`~/EC-trainer`** (★ Downloads 아님)
- **작업 기기 2대**: 맥북 프로 + 맥미니. **양쪽 다 경로는 `~/EC-trainer`**, 동기화는 git (아래 3번)
- 레포: `joonkim-lab/EC-trainer` · 라이브: `joonkim-lab.github.io/EC-trainer`
- 현재 브랜치: **`main`** (curriculum-v2는 역할 끝남 — main과 내용 동일)
- Firebase 프로젝트: `ec-trainer`

---

## 3. 배포 (★ 3권에서 전면 변경)

**GitHub 웹 업로드는 이제 쓰지 않습니다.** 터미널에서:

```bash
cd ~/EC-trainer
git add -A
git commit -m "메시지"
git push origin main
```

push하면 1~3분 뒤 GitHub Pages가 자동 재배포. 확인은 **강력 새로고침(⌘⇧R)** 또는 시크릿 창.
(캐시가 강해서 그냥 새로고침으로는 옛 화면이 남습니다. `?v=2` 같은 쿼리를 붙여도 우회됩니다.)

- **`firestore.rules`만 예외** — git과 별개로 `firebase deploy --only firestore:rules`
  - 인증 만료 잦음 → `firebase login --reauth` 먼저
  - ⚠️ **`concat()` 쓰지 말 것**: 컴파일은 되지만 실행 시 `Missing or insufficient permissions` (실제 사고). 이메일은 각 함수에 **직접 나열**

### ★ 두 기기(맥북 프로 · 맥미니) 운용 규칙

준킴쌤은 **맥북 프로와 맥미니 두 대**에서 작업합니다. 대화창 하나는 컴퓨터 한 대에만 연결되므로, **그 세션이 어느 기기에 붙었는지 먼저 확인**하세요 (`get_device_info`의 `deviceName`). 두 기기를 한 세션에서 동시에 만질 수는 없습니다.

**매 세션 시작할 때 (기기 불문):**
```bash
cd ~/EC-trainer && git pull origin main
```
**매 세션 끝낼 때:**
```bash
git add -A && git commit -m "메시지" && git push origin main
```

- **작업 전 `pull`, 작업 후 `push`** — 이 두 줄이 두 기기를 같은 상태로 유지하는 전부입니다.
- ⚠️ **양쪽에서 동시에 편집하지 마세요.** 한 번에 한 기기에서만. 3권 시절 웹업로드/로컬을 섞어 족보가 갈라진 사고가, 두 기기 사이에서 똑같이 재현됩니다.
- `pull`이 충돌나면 = 반대쪽 기기에 push 안 한 작업이 남아 있다는 뜻. **그 기기로 가서 먼저 push**하고 오세요.
- ⚠️ 저장소를 iCloud/Dropbox 폴더에 두지 마세요 (`.git` 손상 위험). 동기화는 git만 씁니다.

**맥미니에 처음 세팅할 때:**
```bash
cd ~ && git clone https://github.com/joonkim-lab/EC-trainer.git
cd EC-trainer
git config user.name "joon.kim" && git config user.email "joon.kim@educclesia.com"
```

### 왜 바뀌었나 (2026.08.23에 터진 일)

웹 업로드와 로컬 git을 **섞어 쓴 게** 문제였습니다. 웹 업로드는 GitHub 서버에서 커밋을 만들고, 로컬 커밋은 맥에만 쌓입니다. 서로를 몰라서 족보가 갈라졌고, 결과적으로 **애플 디자인·역할 4단·L5·L6가 4개월간 라이브에 안 올라가 있었습니다** (로컬 커밋 4개가 push된 적이 없었음). 커밋 = 내 맥에 저장, push = GitHub로 전송, **둘은 별개**입니다.

지금은 정리 완료 (`a6c5c21` 커밋으로 일괄 반영). **앞으로 한 통로만 쓰면 다시는 안 갈라집니다.**

---

## 4. 커리큘럼 구조 (12레벨 · 3임계점)

### 1차 임계점 · 기초 (L1~L4) — "문장을 만들 수 있다" ✅ 완성
- **L1 뼈대** 6유닛 (SV / be동사 / SVO / SVOO / SVOC / There is)
- **L2 기본 시제** 5유닛 · **L3 기본 조동사** 5유닛 · **L4 늘이고 잇기** 4유닛

### 2차 임계점 · 정교 (L5~L8) — "문장을 정교하게" ✅ 완성
- **L5 시제 심화** 5유닛 (현재완료 경험·결과 / 계속 / **현재완료 vs 과거** / 현재완료진행 / 과거완료)
- **L6 무생물 주어** 4유닛 (사물 주어 / 시간·상황 주어 / **한국어식 vs 영어식** / 수 일치)
- **L7 수동태** 4유닛 (be+p.p. / 시제별 / get·굳어진 표현 / **능동 vs 수동**)
- **L8 준동사+다듬기** 5유닛 (to부정사 / 동명사 / **to파 vs -ing파** / 분사 / wh-의문문·부정)

### 3차 임계점 · 세련 (L9~L12) — "원어민 감각" ⬜ 전부 골격만
L9 뉘앙스 조동사 · L10 비교+관계사 · L11 관계사 심화+분사구문 · L12 가정법+재구성
**여기는 다시 설계 필요** — 12번 항목 참조.

---

## 5. 집필 방법론 (★ 3차도 이 뼈대는 유지)

### 유닛 형식 (고정)
```
익힘카드(사람 집필) → 연습 3문제 → 실전 5문제 (90점 게이트)
```
각 유닛 필드: `id / title / tagline / skeleton / focus / card{achieve,howto,demo} / practice[3] / challenges[5] / similar[2]`

- **tagline**: 개념을 콕 박아 한 줄로
- **card.howto**: 문법 용어 최소화, "누가? → 한다?" 질문식
- **card.demo**: 끊어읽기 시범 (steps로 한 덩어리씩 → answer → tip)
- **practice**: 통과·탈락 없음. steps의 `hint`에 형태 설명 필수
- **challenges**: 통문장 5개. 첫 문제에 `chunks` 넣어 [끊어보기] 지원

### 핵심 원칙 (준킴쌤과 합의)
1. **한국어 신호로 진입** — 판단시키지 말고, 한국어가 알려주는 대로 만들게 함
2. **금지 신호로 울타리** — 오용 방지선을 익힘카드에 명시
3. **마지막에 대비 유닛** — 그 레벨의 판단을 훈련하는 유닛을 하나 둠
   (L5-3 현재완료 vs 과거, L6-3 한국어식 vs 영어식, L7-4 능동 vs 수동, L8-3 to파 vs -ing파)
4. **나선형** — 앞 레벨이 계속 소환됨 (L8-4 분사 = L7 태 판단 + L4 꾸미기 자리)
5. **교재 방법론만 흡수, 예문 복제 금지** (《영어로 문장 만들기 훈련》 유은하, 사람in)

### 레벨별 "벽"과 진입로 (실제로 쓴 것)
- L5 현재완료: 모양이 아니라 **선택**이 벽 → 신호로 진입, 대비로 마무리
- L6 무생물 주어: **발상**이 벽 → "한국어를 먼저 뒤집기"
- L7 수동태: **뼈대가 뒤집힌다**가 벽 → "누가 했는지 모르거나 중요하지 않을 때", 신호는 "~되다/~당하다/~받다"
- L8 준동사: **동사가 두 개**가 벽 → "진짜 동사는 하나, 뒤 동사엔 옷(to/-ing)을 입힌다"
  - to/-ing 선택은 "내가 고르는 게 아니라 **앞 동사가 정한다(궁합)**"로 처리

---

## 6. 규칙값 (RULES, curriculum.js)

```js
PASS_SCORE: 90          // 실전 통문장 게이트
PRACTICE_PER_UNIT: 3
CHALLENGES_PER_UNIT: 5
EXAM_TOTAL: 20          // 레벨 시험 (AI가 매번 새로 출제)
EXAM_MAX_WRONG: 2
EXAM_PASS: 18
EXAM_NEEDS_PERMIT: true // ★ 선생님이 허가해야 응시 가능
PLACEMENT_PASS: 70
```

**레벨 시험 흐름**: 유닛 전부 통과 → "시험 대기" → 선생님이 [🔓 시험 열어주기] → 20문제 → 2개 이하 오답이면 승급. 떨어지면 **허가 소모** + 오답 복습 후 재허가.
(의도: 아이가 혼자 훅훅 올라가지 못하게, 속도를 선생님이 쥠)

---

## 7. 역할 · 보안 규칙 (★ 3권 내용 정정)

**`index.html`은 역할 4단**입니다:

| 역할 | 이메일 | 권한 |
|---|---|---|
| 운영진 | joon.kim@educclesia.com | 전체 |
| 교사 | judy.hong / eunji.ahn / jeein.kang / sol.park @eliteopenschool.org | 학생 관리 |
| 행정 | **미정 (빈 배열)** | 열람만 |
| 학생 | creativemeriel / jasoncanada0506 / elvinkim827 @gmail.com | 학습만 |

→ `ADMIN_EMAILS` / `TEACHER_EMAILS` / `STAFF_EMAILS` / `roleOf()`

**그런데 `firestore.rules`는 역할 2단입니다.** 3권에 적힌 "89줄, 역할 4단 반영, adminEmails()/teacherOnlyEmails()/staffEmails()/studentEmails()" 버전은 **디스크에도 git에도 없습니다** (어딘가에서 유실된 듯). 현재 파일은 82줄이고 `allowedEmails()` / `teacherEmails()` 두 함수만 있어요.

**당장은 문제 없습니다.** 행정 계정이 빈 배열이라 4단이 실질적으로 2단으로 동작하고, 선생님 진도 관리도 정상입니다. **행정 계정을 실제로 만들 때** `firestore.rules`를 4단으로 다시 쓰고 배포하면 됩니다 (그때 `allowedEmails()`에도 그 이메일을 넣어야 로그인이 됩니다 — 여기가 로그인 관문).

---

## 8. 디자인 (애플 / iOS 설정앱 계열)

출처: 준킴쌤의 다른 프로그램 '미리 학습' (`미리_학습_v8-apple.html`)

```
--paper:#F5F5F7  --card:#FFF  --ink:#1D1D1F  --muted:#6E6E73  --line:#D2D2D7
--ok:#1A7F4B  --no:#C4342B
--r:18px  --r-sm:14px
--shadow:0 1px 2px rgba(0,0,0,.04)   --shadow-lg:0 8px 30px rgba(0,0,0,.08)
시스템 폰트 (-apple-system…), 웹폰트 없음
```

**임계점별 포인트 색** (`THEMES` in index.html, `applyTheme()`가 적용):
- 1차 기초 → 블루 `#0B6BCB` · 2차 정교 → 인디고 `#5856D6` · 3차 세련 → 오렌지 `#B25000`

> ⚠️ **L9~L12 집필할 때 확인할 것**: `curriculum.js`의 L9~L12 골격에는 `theme` 값이 핑크(`#db2777`)로 남아 있습니다. L7·L8은 인디고로 맞춰뒀으니, 3차는 오렌지(`#B25000` / deep `#8F4000` / soft `#FDF1E5`)로 통일하세요.

**화면 색으로 배포 확인하는 법**: L1 화면이 **파란색**이면 새 버전, **보라색**이면 캐시된 옛 버전입니다.

크레딧: **made by Educclesia Group** (로그인 화면 + 가이드 2곳)

---

## 9. preview.html (집필용 도구, 신규)

로그인 없이 `curriculum.js`를 그대로 펼쳐 보는 로컬 페이지. 파인더에서 더블클릭하면 열립니다.
레벨 버튼으로 L1~L12 이동, 익힘카드·연습·실전·유사문장·시험 폴백이 **정답·힌트까지 전부 공개**됩니다 (학생 화면 아님).
집필 → 미리보기로 톤 확인 → 다음 레벨, 이 순서가 잘 맞습니다.

---

## 10. Claude와 작업할 때 주의

- Claude는 `~/EC-trainer` 폴더를 직접 읽고 씁니다. 파일 집필·수정은 Claude가, **git은 준킴쌤 터미널에서.**
- 이유: Claude는 이 맥에서 **파일을 삭제할 권한이 없어서** git이 만드는 잠금파일(`.git/index.lock`, `HEAD.lock`)을 못 지웁니다. 쌓이면 `Another git process seems to be running` 오류로 명령이 막혀요.
  - 그 오류가 나면: `rm -f ~/EC-trainer/.git/*.lock`
- git 사용자 정보는 이 repo에 로컬로 설정해 뒀습니다 (`joon.kim / joon.kim@educclesia.com`).
- 파일 수정 후 항상 `node --check curriculum.js`로 구문 검증하고 줄 수·키워드 확인.
- ~~Downloads 이름 충돌 주의~~ → **폴더를 옮겨서 이 위험은 사라졌습니다.**

---

## 11. 미결 · 다음 할 일

1. **L9~L12 (3차 임계점) — 접근법 설계부터** ← 바로 다음, 12번 참조
2. 12레벨 adaptive 배치고사 재설계
3. `firestore.rules` 4단 재작성 + 배포 — **행정 계정을 실제로 만들 때**
4. L3-2 `had better` 유지 여부 (준킴쌤 미결 — 현재는 유지 중)
5. L5 이상으로 올라간 학생 계정 있으면 선생님 패널에서 조정
6. 라이브에 애플 디자인이 처음 올라갔으니, 선생님·학생 계정으로 각각 한 번씩 점검

---

## 12. 3차 임계점(L9~L12) 설계 메모 — 다음 세션 출발점

**왜 지금까지 방식이 안 통하나.** L1~L8은 "한국어 신호 → 뼈대 → 울타리"로 **없던 문장을 만들게** 했습니다. 그런데 L9~L12는 학생이 **이미 만들 줄 아는 문장을 더 낫게 바꾸는** 단계예요. "이 문장 틀렸어요"가 아니라 "이렇게 하면 더 자연스러워요"라서, 정답/오답 게이트(90점)와 궁합이 안 맞을 수 있습니다.

**논의해야 할 것 (준킴쌤 판단 필요):**
- 3차에서도 90점 게이트를 그대로 쓸지, 아니면 채점 기준을 바꿀지
- "대비 유닛" 패턴이 3차에도 유효한지 (여기선 대비가 아니라 **선택지 넓히기**에 가까울 수 있음)
- 진입로를 무엇으로 잡을지 — 후보: **"밋밋한 문장 → 다듬은 문장"의 before/after를 보여주고 고르게 하기**
- L12-4 '문장 재구성'은 사실상 3차 전체의 종합인데, 유닛 하나로 둘지 레벨 전체 성격으로 볼지

**작업 스타일 메모**: 준킴쌤은 한 번에 다 하려는 경향 → **"오늘 여기까지" 스코프 끊기**가 도움됩니다. 레벨 하나 집필 → 미리보기로 톤 확인 → 다음 레벨 순서가 잘 맞습니다.
